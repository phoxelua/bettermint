import React, { PropTypes } from 'react';

const Goal = ({ goal }) => {
  return (
    <div>
      asdfasdf
    </div>
  );
};

Goal.propTypes = {
  goal: PropTypes.object.isRequired,
};

export default Goal;
